{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @workspace/10bottlecash run build",
  "outputDirectory": "dist/public",
  "installCommand": "cd ../.. && pnpm install",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
